# LaTeX build artefacts (always ignored)
*.aux
*.log
*.out
*.synctex.gz
*.toc
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.lof
*.lot
*.nav
*.snm
*.vrb

# PDF: ignored during development.
# At final submission, remove this line and commit the final PDF
# (tagged with `git tag submission-<date>`).
*.pdf

# Local preview renders (PNGs from pdftoppm, used for visual sanity checks)
preview/
